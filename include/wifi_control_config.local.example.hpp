#pragma once

// Copy this file to wifi_control_config.local.hpp (which is ignored), replace
// the placeholders locally, and explicitly build with
// -DRF3_WIFI_CONTROL_ENABLED=1. Never commit the local file.
#define RF3_WIFI_SSID "YOUR_WIFI_SSID"
#define RF3_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
